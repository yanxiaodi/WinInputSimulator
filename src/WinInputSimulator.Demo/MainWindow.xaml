<Window x:Class="WinInputSimulator.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinInputSimulator.Demo"
        mc:Ignorable="d"
        Title="WinInputSimulator Demo" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="WinInputSimulator Demo" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>

        <!-- Description -->
        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,15"
                   Foreground="#555">
            <Run Text="Enter text with special characters below. Click the button to open Notepad and automatically type the text using WinInputSimulator."/>
        </TextBlock>

        <!-- Input TextBox -->
        <GroupBox Grid.Row="2" Header="Text to Input" Margin="0,0,0,15">
            <TextBox x:Name="InputTextBox" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Padding="5"
                     FontSize="14"
                     Text="Hello, World! 🌍&#x0A;Special characters: @#$%^&amp;*()&#x0A;Unicode: Привет, 你好&#x0A;Symbols: ™ © ® € £ ¥"/>
        </GroupBox>

        <!-- Button -->
        <Button x:Name="SimulateButton" 
                Grid.Row="3"
                Content="Open Notepad and Type Text" 
                Height="40"
                FontSize="16"
                FontWeight="SemiBold"
                Click="SimulateButton_Click"
                Background="#007ACC"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"
                Margin="0,0,0,15">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="5"
                                        Padding="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#005A9E"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#004578"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Status TextBlock -->
        <TextBlock x:Name="StatusTextBlock" 
                   Grid.Row="4"
                   Text="Ready" 
                   FontSize="12"
                   Foreground="Green"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>
